# Copy this file to .env.local and fill in your personal values.
# .env.local is gitignored — never commit it.

# Absolute path to your shared Problem Statements folder (e.g. OneDrive-synced SharePoint).
# Leave empty to skip the shared-folder write and save locally only.
PROBLEM_STATEMENTS_SHARED_PATH=
